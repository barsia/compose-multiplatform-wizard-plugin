package io.github.heisiar.composewizard.androidstudio

// NOTE: WizardTemplateProvider requires AS API at compile time
// For now, we use Actions instead which appear in File -> New menu and Welcome Screen
// In the future, we can add wizard integration using runtime reflection

/*
import com.android.tools.idea.wizard.template.Template
import com.android.tools.idea.wizard.template.WizardTemplateProvider

class ComposeMultiplatformTemplateProvider : WizardTemplateProvider() {
    override fun getTemplates(): List<Template> {
        return listOf(composeMultiplatformTemplate())
    }
}
*/

